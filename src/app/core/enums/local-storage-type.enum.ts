export enum LocalStorageType {
    token = 'accessToken',
    refreshToken = 'refreshToken',
    createdTokenTime = 'createdTokenTime',
    userData = 'userData',
    language = 'language', 
    ticket = 'ticket', 
}