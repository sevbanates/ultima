export enum RouteEnum {
    Dashboard = '',
    Login = 'auth/sign-in',
    ForgotPassword = '/forgot-password',

    Logout = 'auth/sign-out',
    ResetPassword = 'auth/reset-password',
    AllClients = '/client',
    ActiveClients = '/active-client',
    InactiveClients = '/inactive-client',
    // Appointment = '/appointment',
    Schedule = '/schedule',
    Message = '/message',
    Inquiry = '/inquiry',
}
